import { SortByColumnPipe } from './sort-by-column.pipe';

describe('SortByColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
